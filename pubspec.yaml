name: easy_todo
description: An easy to use TODO app.


version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^0.5.8
  meta: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true
